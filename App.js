import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';
import Formulario from './components/Formulario';

const App = () => {
  return(
  <>
    <Formulario />
  </>
  )
};

const styles = StyleSheet.create({});

export default App;
